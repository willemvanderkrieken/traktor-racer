services:
  traktor:
    build: .                 # bouwt de nginx-image uit de Dockerfile in deze map
    image: traktor-racer
    restart: unless-stopped
    ports:
      - "80"                 # Coolify koppelt hier automatisch je domein + HTTPS aan
